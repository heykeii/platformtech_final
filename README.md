# platformtech_final
This is our final project for platform technology containing backend services using node.js, express and render.

## Run

Install deps and start the server:

```bash
npm install
npm run start
```

For development with auto-restart:

```bash
npm run dev
```
